import { GameEvent, HubCommand } from "./protocol/protocol";
export interface SessionMetrics {
    initToReadyMs?: number;
    quitToCompleteMs?: number;
    status: "idle" | "booting" | "ready" | "playing" | "terminating" | "completed" | "error";
    startTime?: number;
    endTime?: number;
    error?: string;
}
export interface HandshakeTracker {
    onHubCommand(cmd: HubCommand): void;
    onGameEvent(evt: GameEvent): void;
    getMetrics(): SessionMetrics;
    reset(): void;
}
/**
 * Creates a tracker to measure handshake and lifecycle performance.
 * Use this in the Hub (or test runner) to gather QA KPIs.
 */
export declare function createHandshakeTracker(): HandshakeTracker;
/**
 * Helper to race a promise against a timeout.
 */
export declare function withTimeout<T>(promise: Promise<T>, ms: number, errorMsg?: string): Promise<T>;
/**
 * Standard log format helper for GM/Hub consistency.
 */
export declare function formatLog(level: "info" | "warn" | "error", msg: string, meta?: Record<string, unknown>): string;
//# sourceMappingURL=monitoring.d.ts.map