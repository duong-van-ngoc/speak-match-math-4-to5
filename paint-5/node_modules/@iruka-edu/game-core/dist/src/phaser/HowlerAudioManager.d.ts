export type AudioKind = "bgm" | "voice" | "sfx";
export type AudioFormat = "m4a" | "mp3" | "ogg" | "wav";
export type AudioAsset = {
    key: string;
    kind: AudioKind;
    src?: string[];
    basePath?: string;
    exts?: AudioFormat[];
    volume?: number;
    loop?: boolean;
    preload?: boolean;
    rate?: number;
    crossOrigin?: "anonymous" | "use-credentials" | "";
    probe?: boolean;
    probeTimeoutMs?: number;
};
export type AudioManagerOptions = {
    groupVolume?: Partial<Record<AudioKind, number>>;
    duckBgmTo?: number;
    fadeMs?: number;
    debug?: boolean;
    onState?: (s: {
        unlocked: boolean;
        muted: boolean;
    }) => void;
    /** default cho probe trong register() */
    defaultProbe?: boolean;
    defaultProbeTimeoutMs?: number;
};
export declare class HowlerAudioManager {
    private tracks;
    private unlocked;
    private muted;
    private bgmKey;
    private voiceKey;
    private pendingPlays;
    private queueUntilUnlock;
    private opts;
    constructor(opts?: AudioManagerOptions);
    /**
     * Register assets. Supports:
     * - { src: [...] }
     * - { basePath, exts }  (auto build + optional probe)
     */
    register(assets: AudioAsset[]): Promise<void>;
    has(key: string): boolean;
    loadAll(opts?: {
        strict?: boolean;
        onProgress?: (p: {
            loaded: number;
            total: number;
        }) => void;
    }): Promise<void>;
    unlock(): Promise<boolean>;
    bindUnlockToGesture(target?: HTMLElement | Document): void;
    setMuted(muted: boolean): void;
    isUnlocked(): boolean;
    playSfx(key: string): number | null;
    playVoice(key: string): number | null;
    setBgm(key: string): Promise<void>;
    stopBgm(): void;
    stopAllVoices(): void;
    stopAll(): void;
    pauseAll(): void;
    resumeAll(): void;
    private resolveSrc;
    private play;
    private stop;
    private fadeOutStop;
    private fadeTo;
    private duckBgm;
    private computeTrackVolume;
    private emitState;
    private d;
}
//# sourceMappingURL=HowlerAudioManager.d.ts.map