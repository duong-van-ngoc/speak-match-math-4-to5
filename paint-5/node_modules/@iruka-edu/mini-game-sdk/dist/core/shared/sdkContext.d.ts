export type SdkContext = {
    sessionId?: string;
    gameId?: string;
    lessonId?: string;
    gameVersion?: string;
    sdkVersion?: string;
    hubOrigin?: string;
};
/**
 * Set fallback context (mock) for standalone/test.
 * Safe in hub too: INIT (override) will win.
 * Auto-generate fallback.sessionId if missing.
 */
export declare function configureSdkContext(opts: {
    fallback?: Partial<SdkContext>;
}): void;
/** Override context (typically called from Hub INIT handler). */
export declare function setSdkContext(partial: Partial<SdkContext>): void;
/** Always returns merged context: fallback first, then INIT override. */
export declare function getSdkContext(): SdkContext;
export declare function resetSdkContext(): void;
