export type AudioFormat = "m4a" | "mp3" | "ogg" | "wav";
export declare function buildAudioSrc(basePath: string, exts?: AudioFormat[]): string[];
/**
 * Optional: probe HEAD để lọc file tồn tại (tránh 404).
 * Nếu server không hỗ trợ HEAD, nó sẽ fallback sang "không lọc".
 */
export declare function filterExistingUrls(urls: string[], timeoutMs?: number): Promise<string[]>;
