export type HudOptions = {
    /** container để gắn HUD (default document.body) */
    mount?: HTMLElement;
    /** safe inset (px) từ ScaleManager hoặc CSS env(safe-area-inset-*) */
    safeTop?: number;
    safeLeft?: number;
    zIndex?: number;
};
export declare class ScoreTimerHudDom {
    private root;
    private scoreEl;
    private timeEl;
    constructor(opts?: HudOptions);
    update(snapshot: {
        score: number;
        elapsedMs: number;
    }): void;
    setVisible(v: boolean): void;
    destroy(): void;
}
