export declare function generateUUID(): string;
export declare function generateAttemptId(): string;
export declare function generateIdempotencyKey(action: string, attemptId: string): string;
/**
 * Simple in-memory queue to prevent duplicate submissions locally.
 */
export declare class ResultQueue {
    private seenIds;
    has(attemptId: string): boolean;
    add(attemptId: string): boolean;
    clear(): void;
}
//# sourceMappingURL=idempotency.d.ts.map