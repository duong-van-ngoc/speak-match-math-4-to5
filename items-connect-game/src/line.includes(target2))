const fs = require('fs'); const text = fs.readFileSync('GameScene.ts', 'utf8').split('\n'); const target = 'sdk.progress({ levelIndex: 0, total: 4 });'; const idx = text.findIndex(line =; console.log('progress line', idx !== -1 ? idx + 1 : 'not found'); const target2 = 'this.guideHandShownOnce = false;'; const idx2 = text.findIndex(line =; console.log('guide line', idx2 !== -1 ? idx2 + 1 : 'not found');
