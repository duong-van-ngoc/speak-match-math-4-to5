export type ScoreTimerState = {
    running: boolean;
    startAtMs: number | null;
    elapsedMs: number;
    score: number;
};
export declare class ScoreTimer {
    private state;
    start(nowMs?: number): void;
    pause(nowMs?: number): void;
    reset(): void;
    setScore(score: number): void;
    addScore(delta: number): void;
    getScore(): number;
    getElapsedMs(nowMs?: number): number;
    snapshot(nowMs?: number): {
        score: number;
        elapsedMs: number;
        running: boolean;
    };
    static formatMs(ms: number): string;
}
//# sourceMappingURL=scoreTimer.d.ts.map