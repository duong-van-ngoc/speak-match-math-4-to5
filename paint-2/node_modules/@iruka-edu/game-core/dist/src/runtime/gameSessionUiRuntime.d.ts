import type { GameSdk } from "../game/gameSdk.v1";
import { ScoreTimer } from "./score/scoreTimer";
import { ScoreTimerHudDom } from "./ui/hudDom";
import { ResultOverlayDom } from "./ui/resultOverlayDom";
export type GameSessionUiRuntimeOptions = {
    sdk: GameSdk;
    /** safe insets: lấy từ ScaleManager nếu có */
    safeInsets?: {
        top?: number;
        left?: number;
        bottom?: number;
        right?: number;
    };
    /** gọi khi user bấm replay */
    onReplay?: () => void;
    /** gọi khi user bấm exit (thường sẽ trigger COMPLETE nếu chưa) */
    onExit?: () => void;
};
export declare function createGameSessionUiRuntime(opts: GameSessionUiRuntimeOptions): {
    timer: ScoreTimer;
    hud: ScoreTimerHudDom;
    overlay: ResultOverlayDom;
    start: () => void;
    pause: () => void;
    resume: () => void;
    setScore: (score: number, delta?: number) => void;
    addScore: (delta: number) => void;
    finish: (extras?: any) => void;
    destroy: () => void;
};
//# sourceMappingURL=gameSessionUiRuntime.d.ts.map