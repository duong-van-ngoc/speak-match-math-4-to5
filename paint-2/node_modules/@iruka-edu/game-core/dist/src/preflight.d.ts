import { ManifestValidationReport } from "./manifest";
export interface PreflightOptions {
    /**
     * Base URL where the game is hosted (for resolving entryUrl).
     * E.g. "http://localhost:3000/games/my-game"
     */
    baseUrl: string;
    /**
     * Validated manifest object. If raw JSON is provided, it will be validated first.
     */
    manifest: unknown;
    /**
     * Timeout for handshake in ms. Default 10000.
     */
    timeoutMs?: number;
    /**
     * Container to mount iframe. If not provided, will create a temporary hidden container.
     */
    container?: HTMLElement;
}
export interface PreflightStep {
    name: string;
    status: "pending" | "pass" | "fail" | "warn";
    message?: string;
    details?: any;
}
export interface PreflightReport {
    success: boolean;
    manifestReport?: ManifestValidationReport;
    steps: PreflightStep[];
}
/**
 * Run preflight checks on a game.
 * 1. Validate Manifest
 * 2. Check Network (entryUrl reachable)
 * 3. Smoke Test (Handshake: INIT -> READY)
 */
export declare function runPreflight(opts: PreflightOptions): Promise<PreflightReport>;
//# sourceMappingURL=preflight.d.ts.map