import type { IrukaGame, GameRuntimeConfig, TelemetryEvent, SaveState } from "./types";
export declare abstract class BaseGame implements IrukaGame {
    protected cfg: GameRuntimeConfig;
    onInit(cfg: GameRuntimeConfig): Promise<void>;
    onStart(): Promise<void>;
    onPause(): void;
    onResume(): void;
    onDispose(): void;
    protected telemetry(e: Omit<TelemetryEvent, "ts">): void;
    protected save(payload: Record<string, unknown>): Promise<void>;
    protected load(): Promise<SaveState | null>;
}
//# sourceMappingURL=baseGame.d.ts.map