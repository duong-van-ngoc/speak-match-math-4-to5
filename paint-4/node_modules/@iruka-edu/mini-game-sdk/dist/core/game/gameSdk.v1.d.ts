import { type LaunchContext, type GameErrorCode } from "../protocol/protocol";
export type GameSdkOptions = {
    /** Hub origin hợp lệ. Nên truyền từ query param hoặc hardcode env của hub */
    hubOrigin: string;
    /** Optional: allowlist origin (mặc định chỉ cho hubOrigin) */
    allowlistHubOrigins?: string[];
    /** Hooks */
    onInit?: (ctx: LaunchContext) => void;
    onStart?: () => void;
    onPause?: () => void;
    onResume?: () => void;
    onResize?: (size: {
        width: number;
        height: number;
        dpr: number;
    }) => void;
    onQuit?: () => void;
    /** log */
    onInvalidMessage?: (info: {
        reason: string;
        origin: string;
        data: unknown;
    }) => void;
    onSetState?: (state: any) => void;
};
export type GameSdk = {
    /** gọi khi game sẵn sàng */
    ready: (payload?: {
        capabilities?: string[];
    }) => void;
    /** báo tiến độ loading 0..1 (auto throttle <=10/s) */
    loading: (progress01: number) => void;
    /** update điểm (auto gộp 100ms) */
    score: (score: number, delta?: number) => void;
    /** snapshot tiến độ nhỏ (nhớ <=16KB) */
    progress: (payload: any) => void;
    /** kết thúc phiên */
    complete: (payload: {
        score?: number;
        timeMs: number;
        extras?: any;
    }) => void;
    /** báo lỗi */
    error: (payload: {
        code: GameErrorCode;
        message: string;
        detail?: any;
    }) => void;
    /** destroy listeners */
    destroy: () => void;
    /** thông tin session hiện tại (có sau INIT) */
    getSessionId: () => string | null;
    requestSave: (payload: any) => void;
    requestLoad: () => void;
};
export declare function createGameSdk(opts: GameSdkOptions): GameSdk;
